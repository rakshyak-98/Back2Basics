connection pooling connection borrowing, connection pooling means keeping a reusable set of database connections instead of creating new database connection for every request.
HikariCP (connection pool)

connection - TCP socket over a network
TCP socket - 3 way handshake
latency of connection
SSL TLS handshake
context switching 

[[Hibernate]] ORM framework for Java. allow Java application work with Java object instead of writing SQL for every operation.

generation type sequence
