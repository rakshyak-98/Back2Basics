```bash
cd -; # jump back to last dir OLDPWD;
mkdir -p a/b/c; # make nested dirs in one go.
find . -size +100m; # find files by size.
```

```bash
grep -R --exclude-dir={.git, node_modules} directory; # exclude multiple dirs.
```
