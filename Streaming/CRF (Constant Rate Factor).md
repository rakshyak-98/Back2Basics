CRF is a quality-based encoding mode used by video encoders such as `H/264` (`libx264`)

instead of specifying a fixed bitrate, you specify the desired visual quality. The encoder automatically adjusts the bitrate for each frame to maintain approximately that quality.