- makes code more modular, testable and maintainable by removing hard-coded dependencies between object or modules.

> [!INFO]
> Instead of class creating its own dependencies inside itself (tight coupling), the dependencies are "injected" from the outside.