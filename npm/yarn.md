```bash
yarn install -E <package name>; # install exact version.
yarn upgrade-interactive; # upgrade packages
```

```sh
yarn info;
yarn outdated;
yarn why <package name>;
	yarn cache clean; # clear global cache to free up disk space
```

```sh
yarn upgrade-interactive;
yarn licenses list;
```