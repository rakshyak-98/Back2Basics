> If a program access a memory location now, it is likely to access the same memory location again soon.
**CPU cache** keep recently accessed data because it's likely to be needed again.
**Virtual memory systems** benefit from frequently reused memory pages.
**Database** cache recently queried records.
**Web browser** cache recently visited resources.