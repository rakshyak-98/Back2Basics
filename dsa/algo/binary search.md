- If you are looking for a specific value or a precise insertion point, you should generally use `left <= right`
- if the input halves at each step, its likely `O(logN)` or `O(NlogN)`

> [!INFO]
> When a data set is ordered, you can optimise the search process by making strategic jumps instead of checking every single element sequentially.