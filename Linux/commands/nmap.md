Network mapper -> used for network discovery and security auditing.

- it sends specially crafted network packets to one or more target hosts (IP addresses, hostnames, or ranges) and carefully analyzes the responses. From that analysis it can figure out a lot about what's running on the network.