
```shell
gnome-extensions list; # list installed extensions
gnome-extensions info <extension-name>; # get details of an extension.
gnome-extensions install <path-to-extension.zip>;

gnome-extensions enable <extension-name>;
gnome-extensions disable <extension-name>;

gnome-extensions reset <extension-name>; # resent an extension to default settings.
```