Java doesn't support Multiple inheritance, If both `A` and `B` provide the same method, Java would have to determine which implementation `C` should inherit.

"Java doesn't support multiple inheritance of classes because it can create ambiguity when multiple parent classes contain the same method. To avoid this complexity, Java supports multiple inheritance through interfaces."

Instead java allow a class to implement **multiple interfaces**