> [!INFO]
> - [Git hooks](https://git-scm.com/book/en/v2/Customizing-Git-Git-Hooks) → run locally on your machine (or on the Git server if server-side) as scripts during Git operations like commit, push, rebase, etc. 

## Subscribe to one branch for auto pull 